import React from 'react';
import ReactDOM from 'react-dom';
import App from './App.js'; // This is where your main component lives

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
