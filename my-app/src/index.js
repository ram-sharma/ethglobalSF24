import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Optional: You can create this for styling, or ignore this line if not needed
import App from './App'; // This is where your main component lives

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
